// ES module view of protobufjs/minimal for the Studio client, which does
// `import * as _m0 from "protobufjs/minimal"`. Node cannot detect the names a
// CommonJS module attaches at runtime, so re-export them explicitly here.
import m from "protobufjs/minimal.js";
export const { build, Writer, BufferWriter, Reader, BufferReader, util, rpc, roots, configure } = m;
export default m;
